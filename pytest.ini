[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --screenshot=only-on-failure

markers =
    smoke: quick checks against the configured base URL

# Video/tracing defaults come from config/settings.yaml (CLI: --video, --tracing).
# Playwright best-practice examples:
#   pytest --headed --slowmo=500
#   pytest -m smoke
