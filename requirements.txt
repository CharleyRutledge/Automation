# UI automation stack (install browsers: python -m playwright install chromium)
playwright>=1.58.0,<2
pytest-playwright>=0.7.2,<0.8
pytest>=8.0.0,<9
pytest-html>=4.1.0,<5
PyYAML>=6.0.1,<7

# AI summaries + Telegram (optional; enable in config/settings.yaml)
anthropic>=0.49.0,<1
requests>=2.32.0,<3

# PTA model (trace equivalence detection)
imagehash>=4.3.1,<5
Pillow>=10.0.0,<12
networkx>=3.0,<4
numpy>=1.24.0,<3
